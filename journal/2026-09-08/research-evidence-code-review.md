# 研究证据改造：提交前审查

本轮按当前工作区及实施计划审查并整理本地提交。重点覆盖请求约束、证据身份和引用、模型校验返回值、预算预约与动作恢复、SQLite 核心提交与交付隔离、版本产物及消费者，以及兄弟仓库 Google 结果卡片解析。保留此前已完成且被本次改造依赖的结果事务、模块拆分和 Wiki 检索工作。

## 已发现并修复

| 优先级 | 问题与触发条件 | 修复与验证 |
|---|---|---|
| P1 | 模型对同一 claimId 返回重复判断时，主张校验通过存在性检查，再取第一条；可忽略另一个冲突判断。表达校验也未要求一一对应。 | 两阶段均要求 ID 精确覆盖且唯一。重复、缺失、额外 ID 进入原有有限 parse 重试，不增加重试预算。新增主张与表达重复判断回归，修复前均可复现错误接受。 |
| P1 | 判断同时包含 supported 和有效 counterPassageIds 时，代码保存了反证，却仍将主张判为支持并发布。 | 通过合法性检查的显式反证使最终 verdict 为 conflicting，同步降低任务绑定并保留锚点。新增自相矛盾返回值回归，修复前可复现错误发布。 |
| P2 | 新 revision 保留旧引用注册项时，uniqueCitationCount 把未展示的旧编号也当作主报告实际引用。 | 分开记录 displayed uniqueCitationCount 与 citationRegistryEntryCount。零可发布主张、但保留证据引用编号的回归验证实际引用为零。 |

校验语义升级为 claimReviewVersion=3。新版本未完成报告不能复用旧校验结果；新增恢复测试证明会重新验证主张、保持 claim ID，并且不搜索。同版本报告恢复仍只生成表达；已完成研究的 commit-only 路径继续交付原 revision，不修改历史结果。

本轮未发现尚未处理的阻断提交问题。语义判断本身仍依赖模型，有限比较片段不能证明所有事实均已核实；此前 A 不完整和 B 的调查缺口继续保留。

## 验证

- 主仓库完整测试 952 项通过：引擎 683、Wiki 20、应用 249；ESLint、Vite build 通过。
- Google skill 36 项测试、manifest 一致性检查及变更文件 ESLint 通过；无新增发现/调用协议变更，因此不扩大到整个平台运行时改造。
- 暂存区 `git diff --cached --check` 通过；清理了此前未跟踪模块末尾多余空行。
- 提交候选路径未包含 .env、运行数据、会话产物或生成站点；候选文件未检出常见密钥格式或私钥头。此检查不替代凭据管理。
- 本轮未重新执行付费/长时间在线调研。此前 984369 / 604661 个已确认探索 token 的实跑及交付审计仍作为已有记录；本次新校验边界由可复现 fixture 回归验证。

## 提交组织

| 仓库与分支 | 提交 | 范围 |
|---|---|---|
| js-deepresearch-agent / codex/research-evidence-redesign | e499c7b | 请求与证据改造、可恢复执行和结果交付、消费者适配及回归。模块间互相依赖，作为一个完整可运行实现提交。 |
| js-deepresearch-agent / codex/research-evidence-redesign | 随本文档提交 | README、Agent 指南、设计/实跑/审查记录。 |
| js-eyes / codex/google-result-snippets | a9798b5 | Google 卡片内摘要、空描述、去重及 skill 1.0.1 的一致版本与测试。 |

均为本地提交，未推送远端。兄弟仓库原有 js-x-ops-skill/SKILL.md 改动不纳入提交，并以内容哈希验证保持原样。
