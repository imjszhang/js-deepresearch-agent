Local test-only TLS certificate and private key copied from the Undici v7.25.0
test fixtures (MIT). They are not trusted or used by production code; the pair
exists only to exercise HTTP/2 ALPN against a loopback server without network
access.
